# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=votevox
DB_USER=votevox
DB_PASSWORD=votevox_secret

# JWT — must be at least 32 characters in production
JWT_SECRET=change-me-in-production-must-be-at-least-32-chars-long

# pgAdmin
PGADMIN_EMAIL=admin@votevox.at
PGADMIN_PASSWORD=admin
